import { MaxDescribeLenghtPipe } from './max-describe-lenght.pipe';

describe('MaxDescribeLenghtPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxDescribeLenghtPipe();
    expect(pipe).toBeTruthy();
  });
});
